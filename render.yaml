services:
  - type: web
    name: airquality-intelligence
    runtime: python

    buildCommand: |
      pip install -r requirements.txt

    startCommand: |
      streamlit run app/dashboard.py --server.port=$PORT --server.address=0.0.0.0